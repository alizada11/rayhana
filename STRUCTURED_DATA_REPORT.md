# Structured Data Report

## Implemented

- Added server-rendered JSON-LD injection support in the shared SEO system.
- Added `Product` structured data on the new product detail route.
- Included aggregate rating and offer data based on current product records.

## Still Needed

- `Organization` and `WebSite` sitewide schema.
- `BreadcrumbList` for product and blog routes.
- `BlogPosting` schema on blog detail pages.
- `FAQPage` schema for the help/FAQ surface.
- `LocalBusiness` schema for contact/location pages.

## Validation Target

- Test rendered JSON-LD with Google Rich Results and schema validators after deployment.

